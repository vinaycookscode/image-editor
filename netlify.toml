[build]
  publish = "dist/image-editor/browser"
  command = "npm run install:netlify && npm run build:netlify"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 